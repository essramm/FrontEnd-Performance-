const { shortListURLs } = require('./shortlist-urls');
const { invalidURLs } = require('./invalid-urls');
const { urls } = require('./urls');

module.exports = {
  shortListURLs,
  invalidURLs,
  urls
};